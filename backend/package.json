{
  "name": "omo-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../.env src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/runtimeConfig.js && node --check src/cardService.js && node --check src/sourceVerifier.js && node --check src/store.js",
    "docs:check": "node ../tools/check-wikilinks.mjs",
    "test": "node --test",
    "test:all": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
