# Recallo 2.0 功能与交互说明文档（合并版）

> 版本：合并草案 v0.1  
> 合并日期：2026-07-25  
> 状态：产品方向讨论中，非最终实施规格  
> 来源：`recallo-2-prd-draft.md`、`Recallo_功能与交互说明文档_v1.0(1).md`  
> 合并原则：共同结论统一表述；互补内容纳入同一结构；冲突方案并列保留并标记为“待确认”，不在本文件中擅自裁决。

---

## 0. 文档使用说明

本文档用于产品、设计、iOS、后端和测试共同对齐，分为四个层次：

1. 产品定位与原则：解释为什么做、MVP 验证什么；
2. 功能与系统规则：定义捕获、处理、知识卡、抽取、唤醒、通知和删除；
3. 全局与页面交互：供设计走查、路由实现和测试用例使用；
4. 冲突与开放问题：保留两份源文档中的不同方案，待产品决策后再转为实施合同。

凡标记为“待确认”的内容都不得视为开发承诺。若本文件内部出现未被冲突表覆盖的歧义，应回到两份源文档核对，不得默认选择实现成本更低或视觉更完整的一方。

### 0.1 决策状态

| 标记 | 含义 |
|---|---|
| 已确认 | 两份文档一致，或产品草案明确写明已确认 |
| 合并结论 | 两份文档互补，可同时成立 |
| 待确认 | 两份文档存在直接冲突或 MVP 范围不一致 |
| 后续能力 | 有价值，但不作为当前 MVP 默认范围 |

---

## 1. 产品定位

### 1.1 问题陈述

用户每天会从短视频、公众号、网页、社交媒体和信息流中遇到少量真正有价值的观点、方法、案例和知识。现有产品通常要么保存完整内容，让它沉没在收藏夹和截图相册中；要么把内容转化为课程、章节和大量题目，产生新的学习负担。

真正的痛点发生在未来需要调用知识时：用户知道自己曾经见过相关内容，却只剩“我好像看过”的模糊印象，无法准确表达、判断或应用。

```text
信息流中偶然遇见
→ 当下觉得有价值
→ 截图或收藏后继续滑动
→ 在无感知中逐渐遗忘
→ 真正需要使用时只剩模糊印象
→ 无法调用
```

截图是用户已经形成的低成本保存习惯，也是主观价值信号。但截图行为可能造成认知卸载：用户认为设备已经替自己保存，之后很少重新打开，也没有进一步理解和记忆。

### 1.2 一句话定义

> Recallo 捕获真正吸引你的那一点，并在未来把它重新变成你能够理解、想起和使用的知识。

### 1.3 核心转向

```text
从：完整内容 → 课程 → 单元 → 大量题目
到：高价值截图 → 恢复语境 → 一张核心知识卡 → 抽取 → 唤醒 → 揭示 → 再见
```

### 1.4 用户目标

- 用极低成本保存当下最有价值的内容；
- 不需要自己分类、命名、摘录和制题；
- 愿意重新打开产品，遇见过去真正重视的内容；
- 不只产生“我见过”的熟悉感，而是重新辨析核心意义；
- 在未来交流、判断和行动时更容易调用这些知识。

### 1.5 产品验证目标

- 验证截图能否成为稳定的高价值内容输入；
- 验证抽卡式期待感能否提高用户开始二次唤醒的意愿；
- 验证语义刮开能否以较低负担促使用户主动回想；
- 验证知识价值和稀有度能否增加期待而不伤害信任；
- 验证动态短回合是否适合不同规模的早期卡池；
- 验证用户能否在跨时段后重新想起并在真实场景调用知识。

### 1.6 产品原则

- 不帮助用户记住看过的一切，只处理用户主动保存的高价值局部；
- 保存动作保持轻量，把整理复杂度转移给系统；
- AI 的第一职责是理解、溯源和建立上下文，不是简单看图总结；
- 先证据，再生成；不把猜测来源伪装成事实；
- 一张截图默认只形成一张核心知识卡，不扩展成章节和课程；
- 二次唤醒不能退化成被动重看，也不能重新变成重型学习；
- 游戏感服务于期待和认知动作，不制造付费概率、损失厌恶或任务欠账；
- 来源可信度、知识价值、当下相关性和唤醒需求分别建模；
- 不展示虚假百分比、虚假剩余时间或无法保证的技术进度；
- “想偏了”和“没想起”不是失败，不给予惩罚；
- 用户拥有自己的数据、节奏和退出选择。

---

## 2. 术语对齐

| 场景 | 术语 | Recallo 中的含义 |
|---|---|---|
| 页面间跳转 | 导航 Navigation | 任意页面 A → 页面 B |
| 页面动画 | 转场 Transition | Push、Modal、淡入淡出等动画表现 |
| 操作方式 | 手势 Gesture | 点击、长按、滑动、拖拽、刮开 |
| 引发动作的控件 | 事件触发器 Trigger | 按钮、卡片热区、图标、通知、Toast 链接 |
| 临时任务界面 | Modal / Sheet | 收件、确认、筛选、反馈等临时流程 |
| 内容纵深浏览 | Push / Pop | 卡片详情、历史事件、平台列表等 |
| App 外进入指定内容 | 深层链接 Deep Link | 通知点击后打开对应卡片或事件 |
| 数据呈现阶段 | 状态 State | 初始、加载、成功、空、失败、离线、需确认 |
| 即时响应 | 反馈 Feedback | 按压态、Toast、触感反馈 |
| 轻量增强 | 微交互 Micro-interaction | IP 动作、辉光、落灰恢复等 |
| 记忆碎片 | Fragment | 只有截图证据、来源或上下文尚未补全的可复习内容 |
| 正式知识卡 | Formal Card | 来源匹配、上下文和质量门槛通过后的知识卡 |
| 承重语义 | Hidden Semantic | 缺失或记错会实质改变知识意义的概念、关系、条件或结论 |
| 自我校准 | Assessment | 用户揭示后报告“没想起 / 想偏了 / 想对了” |

---

## 3. 产品信息架构

### 3.1 核心对象

```text
Capture 截图捕获
→ Processing Task 处理任务
→ Source Binding 来源绑定
→ Memory Fragment / Knowledge Card
→ Recall Session 唤醒回合
→ Assessment 自我校准
→ Schedule 下次唤醒状态
```

一张知识卡至少包含：

- 原截图或其可回溯引用；
- 核心知识表述；
- 刮开前可见的唤醒线索；
- 被遮盖的承重语义；
- 核心意义、适用条件和边界解释；
- 必要来源上下文；
- 来源与证据状态；
- 知识价值分析与稀有度；
- 内容版本和时间有效性；
- 唤醒调度状态。

### 3.2 顶层导航

两份文档对顶层信息架构描述不同，当前保留以下方案：

**方案 A：MVP 极简结构（产品草案倾向）**

- 首页以动态 IP 为唯一主操作；
- 统一卡池，不要求用户在抽取前选择主题、平台或关键词；
- 来源、原截图、稀有度原因和管理能力放在次级信息层；
- 卡牌收藏、搜索和管理边界尚未确认。

**方案 B：完整移动端结构（交互说明方案）**

- 「今天」：回合入口、处理中状态、新发现和中断恢复；
- 「知识云」：云图、时间、平台、搜索与筛选；
- 「提醒与设置」及卡片详情、时间相册等纵深页面。

最终 Tab 数量、知识云是否进入 MVP、首页是否展示“新发现”等内容待确认。

---

## 4. 核心功能规则

### 4.1 截图捕获与可靠收件

已确认 MVP 以截图为核心输入，基础链路为：

```text
截图
→ 系统分享
→ Recallo Share Extension
→ 可靠保存截图或本地待上传记录
→ 创建处理任务
→ 自动触发溯源 workflow
→ 用户立即返回原 App
```

只有满足以下任一条件，Share Extension 才能显示“已收下”：

1. 服务端已经接收截图与最小任务信息；
2. 设备已经建立可恢复、可幂等重试的本地待上传记录。

两者都失败时必须明确显示“未保存，请重试”，不能提前关闭并伪装成功。

共同要求：

- 默认不要求裁剪、分类、命名或解释为什么重要；
- 截图上传成功后自动开始处理，不需要再次确认；
- 后台失败不阻断用户继续浏览原内容；
- 离线、弱网、App 关闭、设备重启后仍能恢复已确认收件的记录；
- 重试复用同一捕获记录，避免生成重复任务和重复卡片。

**待确认冲突：Share Extension 是否要求选择主题**

- 方案 A：不要求主题，保存动作优先；
- 方案 B：Sheet 中提供 8 个一级主题单选，但主题为空也不阻断提交。

### 4.2 处理生命周期

```text
已接收
→ 正在整理
   ├─→ 正式知识卡
   ├─→ 记忆碎片
   └─→ 自动重试中
         ├─→ 正式知识卡 / 记忆碎片
         └─→ 暂未整理 → 用户重试 / 删除
```

用户界面可以把“已接收、正在整理、自动重试中”合并表达为“正在整理”，不逐项暴露 OCR、检索、提取、模型生成等内部阶段。

必须区分：

- 来源没有找到：正常业务结果；若截图证据足够，生成记忆碎片；
- 处理任务异常：文件损坏、服务不可用、任务中断等技术问题；先有限自动重试；
- 暂未整理：重试耗尽且截图也不足以安全生成；允许用户重试或删除。

首页状态规则：

- 有可抽卡：抽取入口正常可用，处理中任务只占次级入口；
- 无可抽卡但有处理中记录：显示“正在整理，可以先离开”，不伪造卡片；
- 无卡且无处理记录：显示首次保存引导；
- 有暂未整理记录：从同一状态入口提供重试和删除。

### 4.3 AI 理解、溯源与上下文化

```text
截图与系统上下文
→ OCR / 视觉理解 / 平台识别
→ 候选来源发现
→ 多证据验证
→ 原文 / 视频 / 字幕 / 视觉内容提取
→ 定位截图片段
→ 建立完整语义上下文
→ 推断用户关注点
→ 生成一张核心知识卡
```

正式知识卡不能只依据 OCR 文本或局部段落。系统应先理解来源整体主题、论证、前提、结论及截图片段在其中的作用，再输出最小必要上下文。

自动溯源失败时生成记忆碎片：

- 只使用截图明确支持的文字和画面；
- 不补充截图之外的因果、立场和结论；
- 明确标记“仅根据截图生成”；
- 可以参与唤醒，但不获得 R / SR / SSR；
- 后台可继续尝试溯源；
- 用户可以补充链接。

用户补充链接后，系统必须验证截图内容确实存在于候选来源。主题相关、标题相同、作者相同或链接暂时无法访问，都不等于匹配成功。

**待确认冲突：多候选来源如何处理**

- 方案 A：对手动链接只输出“匹配成功 / 未匹配”，未定位到截图内容就保持记忆碎片；
- 方案 B：后端可以返回两个候选来源，由用户选择来源 A、来源 B 或“先只保存截图”。

### 4.4 记忆碎片升格

```text
验证通过的来源
→ 完整来源理解
→ 定位截图片段
→ 重建核心知识与承重语义
→ 通过质量门槛
→ 记忆碎片升格为正式知识卡
→ 独立评估 R / SR / SSR
```

升格表示来源与上下文补全，不保证稀有度提高。新版本可能改变卡片表述，因此必须：

- 保留此前的抽取和反馈历史；
- 明确提示卡片已经补全；
- 给新版本一次出现机会；
- 不把旧版本连续“想对了”直接当作新版本已掌握。

### 4.5 知识卡生成与质量门槛

每张截图默认只生成一个核心知识卡。截图是用户注意力锚点，不是默认卡面主体。

承重语义优先选择：

- 因果机制；
- 关键条件与边界；
- 对比关系；
- 方法原则；
- 核心结果。

不得机械遮盖普通名词、语法上唯一可猜的词、无关数字、无意义专有名词或来源不支持的结论。遮盖范围由语义完整性决定，不按固定字符数、词数或句长比例决定。

若无法生成合格承重语义：

1. 在不改变来源含义的前提下改写核心知识；
2. 有限重试后仍失败，降级为“语境线索 + 整句核心知识刮开”；
3. 不无限重试、不随机挖词，也不让卡片消失。

### 4.6 知识价值与稀有度

```text
来源完整度 → 记忆碎片 / 正式知识卡
知识价值   → 正式卡 R / SR / SSR
当下相关性 → 统一卡池中的辅助权重
唤醒需求   → 抽取优先级与顺序
```

价值维度：

- P：Personal Relevance，个人相关性；
- U：Utility，改善判断或行动的能力；
- T：Transfer Leverage，跨场景迁移能力；
- I：Information Gain，相对已有认知的增量；
- D：Durability，价值持续时间；
- F：Frontier Leverage，提前理解新范式的潜在杠杆。

稀有度语义：

- R：可靠且值得保留，复用范围较窄；
- SR：能够改善一类判断或行动，可重复使用；
- SSR：对个人具有高杠杆，可改变重要判断或迁移到多个场景。

稀有度是卡面视觉属性，不是独立开奖步骤。卡片出现时即显示真实标签，并立即允许进入核心唤醒交互。R 不能使用失败、破碎或负面效果；记忆碎片显示中性未补全状态。

### 4.7 今日卡池与调度

系统使用统一卡池，不把领域、主题或平台拆成多个强制入口。今日候选优先考虑：

- 已到唤醒窗口的卡；
- 尚未完成首次回顾的新卡；
- 长期未出现但仍有价值的卡；
- 少量用于意外重逢的合格卡。

新卡和刚升格的卡生成后立即可抽，不设置固定 24 小时或次日门槛。

最小调度状态包括：上次揭示时间、上次自我校准、连续“想对了”次数和下一次优先唤醒时间。用户侧不展示到期日、窗口名、连续次数、欠账或补做任务。

| 本次结果 | 调度含义 |
|---|---|
| 没想起 | 连续成功次数归零，进入近期窗口 |
| 想偏了 | 连续成功次数归零，进入近期窗口，修正优先级不低于遗忘 |
| 第一次或中断连续后的想对了 | 进入中期窗口 |
| 连续多次想对了 | 逐步进入远期窗口 |
| 揭示后未反馈 | 进入未知窗口，不视为想对了 |

只锁定“近期 < 未知 < 中期 < 远期”的顺序，具体时长待数据校准。稀有度只能在唤醒需求接近时作为次级信号，不能让未到期 SSR 挤掉真正需要唤醒的 R。

### 4.8 抽取与回合

产品草案的默认规则：

- 首页不提供单抽 / 十连选择；
- 点击主入口后抽取 `min(10, 当前可抽取独立卡数)` 张；
- 不足十张时抽出全部，不重复卡片凑数；
- 同一回合内不重复同一张卡；
- 回合集合与顺序确定后，除删除外不在回合中重排；
- 所有抽取免费，不消耗金币、体力、抽卡券或付费次数；
- 今日高优先级内容完成后，可以继续自由抽取；
- 未完成今日候选不产生任务欠账、断签或惩罚。

卡叠最多显示四层后续卡背，并显示真实进度。下一张可以透出与真实稀有度一致的克制辉光，但辉光只是装饰，不阻断当前操作、不制造保底或概率暗示。

**待确认冲突：回合入口、暂停和结束**

- 方案 A：点击动态 IP 直接形成回合；没有开始页、产品内关闭按钮、暂停确认或结算页。用户通过系统级离开中断，回来后直接恢复原卡；最后一张完成后自然返回极简首页。
- 方案 B：先进入“回合开始页”，显示时长、张数和摘要；回合中提供“暂停”，退出需要确认；最后进入结算页，展示张数、用时和掌握变化。

### 4.9 语义刮开与自我校准

核心认知循环：

```text
看到可理解但不完整的知识表述
→ 在脑中尝试恢复承重语义
→ 刮开或点击揭示
→ 对照完整知识与解释
→ 自我校准
→ 进入下一张或结束
```

交互要求：

- 不要求输入或逐字复述；
- 单卡只需一次短手势；
- 覆盖达到阈值后自动完整揭示；
- 提供与刮动等价的点击揭示；
- 覆盖层使用中性材质，不通过材质泄露稀有度；
- 正文可滚动，底部交互区避开安全区；
- Dynamic Type、VoiceOver 和 Reduce Motion 下保留等价流程；
- 触控目标不低于 44pt。

三档状态固定为：

- 系统状态：忘记 / 记偏 / 记住；
- 移动端口语：没想起 / 想偏了 / 想对了；
- 从左到右顺序固定，不增加第四档；
- 不将自我报告包装成客观成绩，也不奖励某一档诱导虚报。

产品草案中的滑杆方案：滑块初始位于轨道外休止位，三个节点都是有效答案；吸附时轻震，节点上松手即提交并自动进入下一张；拖回休止位或未吸附松手则取消并停留当前卡。

**待确认冲突：题型与反馈层**

- 方案 A：MVP 只验证语义刮开，不并行提供判断题、选择题、传统闪卡；提交滑杆后自动进入下一张，不出现“下一张”按钮或单卡反馈 Sheet。
- 方案 B：同时支持闪卡、判断题、选择题；完成自评后弹出 40% 高度反馈层，展示结果和回响值，并由用户点击“下一张”。

### 4.10 问题通知与定向唤醒

通知由具体卡片的承重语义生成。点击后通过版本化深链直接打开同一张待揭示卡，不经过首页或普通抽取。

共同规则：

- 问题与答案严格语义匹配；
- 不提前泄露答案；
- 不使用羞耻、催促、签到和欠账文案；
- 删除、升格或重生成导致语义变化时，旧通知必须失效；
- 每个本地自然日最多实际送达一条；
- 没有合格候选时宁可不发送；
- 忽略通知后当天不补发、不惩罚；
- 首次冷启动不请求通知权限；至少完成一次揭示和自我校准后，再通过价值说明由用户主动触发系统权限；
- 默认锁屏使用不含具体知识的通用文案；用户可主动开启具体问题预览；敏感内容始终降级为通用文案。

**待确认冲突：整理完成通知**

- 方案 A：MVP 不逐条发送“生成完成”系统通知，通知只服务于后续定向唤醒；
- 方案 B：支持“整理完成”通知，点击后进入新知识卡详情。

### 4.11 删除与数据控制

每张正式卡和记忆碎片在次级菜单提供“删除这张卡”，删除前明确确认。删除应移除用户账户下对应的：

- 原截图；
- 来源关联；
- 生成内容；
- 复习历史；
- 当前调度；
- 未送达通知。

连续回合中删除当前卡后直接进入下一张，不补入替代卡、不改变剩余顺序。MVP 不要求用户选择删除原因、填写正确答案或帮助训练 AI。

---

## 5. 全局交互规范

### 5.1 转场

| 场景 | 推荐方式 | 备注 |
|---|---|---|
| 一级 Tab 切换 | 直接切换 | 0 ms |
| 内容纵深页面 | Push | 支持系统返回手势 |
| 临时任务 | Modal Sheet | 收件、确认、筛选、编辑 |
| 唤醒回合 | 全屏 Modal 或全屏状态切换 | 是否有独立开始页待确认 |
| 单卡反馈 | 40% Sheet | 仅方案 B 保留 |
| 关闭与返回 | 与进入对称 | 回合内规则待确认 |

### 5.2 手势

| 手势 | 默认行为 | 例外 |
|---|---|---|
| 单击 | 触发主要动作 | 刮开区域点击可直接揭示 |
| 长按 | 复制、收藏、反馈、查看来源 | 适用于 AI 生成文本 |
| 屏幕左边缘右滑 | 返回上一 Push 页面 | 回合内是否拦截待确认 |
| Sheet 下滑 | 关闭临时任务 | 未可靠保存的收件 Sheet 关闭前需确认 |
| 刮动 | 移除语义覆盖层 | 提供点击揭示替代 |
| 拖拽滑杆 | 三档自我校准 | 仅产品草案方案 |

### 5.3 异步状态

所有异步页面必须覆盖：初始、加载、成功、空、失败、离线；处理任务还需覆盖需确认、自动重试和暂未整理。

- 优先展示骨架或已有缓存，不显示空白页；
- 文字优先于复杂图表；
- 不显示无实际依据的百分比和倒计时；
- 失败文案保持非指责，并提供重试或替代路径；
- 离线时允许浏览缓存，并明确本地排队状态。

### 5.4 反馈与微交互

| 操作 | 建议反馈 | 时长 |
|---|---|---|
| 主按钮按压 | 缩放至 0.98，阴影降低，Light Impact | 120 ms |
| 收件成功 | 底部 Toast + 轻触反馈 | 2 秒 |
| 滑杆吸附 | 轻震 | 即时 |
| 自评确认 | 清晰但克制的确认震动 | 即时 |
| 删除 | 二次确认 + Warning 反馈 | 常驻直到选择 |
| 撤销 | 带“撤销”的 Toast | 5 秒 |

交互说明文档提出“答对时翠绿描边、黄色绒光和 IP 短跳”；产品草案强调不奖励某个自评结果。该动效仅在保留客观题型的方案 B 中成立，不能套用到“想对了”自报结果上。

### 5.5 无障碍

- 所有主要触控目标不低于 44pt；
- 支持 Dynamic Type，正文超出时滚动而非缩小或截断；
- VoiceOver 顺序遵循“知识语境 → 揭示 → 解释 → 反馈”；
- 刮动提供点击揭示替代；
- 滑杆支持逐档调节和直接点选；
- 稀有度不能只通过颜色表达；
- Reduce Motion 下移除位移、颗粒和复杂辉光，但保留状态变化和内容显现。

### 5.6 二次确认

必须二次确认：

- 删除截图、知识卡或复习记录；
- 关闭尚未可靠保存的 Share Extension；
- 若最终采用方案 B，回合中选择“退出”而非“暂停”。

其余低风险操作尽量一次点击直达。

---

## 6. 功能流程

### 6.1 截图导入到知识卡

```text
用户截图并打开系统分享
→ Share Extension 预览
→ 可靠保存或写入离线队列
→ 显示已收下并关闭
→ 后台视觉理解与来源恢复
→ 生成正式知识卡 / 记忆碎片 / 需确认 / 暂未整理
→ 首页状态刷新
→ 用户按需查看、确认、重试或删除
```

### 6.2 首页到唤醒

```text
打开首页
→ 恢复未完成回合，或读取今日候选
→ 用户触发主入口
→ [可选冲突方案：回合开始页]
→ 卡叠出现
→ 逐张进行预测—揭示—自评
→ 自动下一张或反馈层后下一张
→ 最后一张完成
→ 极简首页或回合结算页（待确认）
```

### 6.3 来源确认

```text
来源证据不足
→ 保持记忆碎片
→ 自动继续尝试，或用户补充链接
→ 验证截图内容是否存在于来源
→ 匹配成功：升格正式卡
→ 未匹配：保持碎片并允许重试
```

若最终采用候选选择方案，则在“证据不足”后增加候选 A / B / 只保存截图分支，但候选被选中后仍必须完成证据校验。

### 6.4 定向问题通知

```text
调度器选择有效卡片
→ 生成与承重语义匹配的问题
→ 隐私与质量检查
→ 当日本地时区送达上限检查
→ 发送通用或具体问题预览
→ 点击后打开版本匹配的目标卡
→ 揭示并自评
→ 退出或回到首页继续普通抽取
```

---

## 7. 页面级交互说明

### 7.1 首次启动引导（后续能力或待确认）

- 交互说明方案为三屏全屏引导，无 Tab Bar；
- 屏间水平滑动，第 3 屏进入首页并替换导航栈；
- 通知权限不在冷启动请求，应延迟到用户完成一次核心体验之后；
- 相册权限仅在 App 内主动选择相册导入时请求。

### 7.2 Share Extension 收件页

结构：截图缩略图、简短说明、主 CTA、隐私入口；主题选择是否出现待确认。

状态：提交前 → 提交中 → 已可靠保存 → 失败可重试 → 离线已排队。

成功后关闭扩展，停留在原 App，不强制打开 Recallo。缩略图保持比例，字段缺失不应阻断保存。

### 7.3 整理状态列表

从首页“正在整理”次级入口 Push 进入。列表项可展示缩略图、聚合状态和次要操作。状态建议收敛为用户可理解的：

- 正在整理；
- 已完成；
- 记忆碎片；
- 需要确认；
- 暂未整理。

不把 OCR、检索、生成等内部阶段作为主文案。状态变化采用行级淡入，不整页闪烁刷新。

### 7.4 首页「今天」

共同状态：加载、有可抽卡、处理中但无卡、完全空、存在中断回合、今日高优先级已完成、离线。

**方案 A：极简首页**

- 动态 IP 是唯一主操作；
- 根据可抽卡、处理中、空状态切换姿态；
- 规则、处理中列表和卡池详情为克制的次级入口；
- 不常驻新发现、分类、保底说明和多张运营卡片。

**方案 B：模块化首页**

- 问候语、IP 状态区、回合主卡；
- 条件展示“上次停在这里”“正在整理”；
- 展示“今天的新发现”横滑列表，最多六张；
- 铃铛进入通知中心。

### 7.5 回合开始页（冲突保留）

方案 B 使用全屏 Modal，展示 IP、预计时长、卡片张数、内容摘要和“开始这一回合”；最多十张，卡池不足时显示实际数量。方案 A 明确不设置此页，点击首页 IP 后直接进入卡叠。

### 7.6 唤醒卡片

共同结构：真实进度、卡片等级或碎片状态、知识线索、揭示区、解释和次级来源入口。

方案 A 以语义刮开为唯一主要认知交互，并使用底部三节点滑杆；方案 B 允许闪卡、判断题和选择题，底部操作随题型变化。两者不得在未决策前混合实现为必做链路。

### 7.7 单卡反馈层（冲突保留）

仅方案 B 使用 40% 高度 Sheet，展示答题与自评组合结果、回响值、来源入口和“下一张”。方案 A 不使用反馈层，提交自评后下一张自动升到顶层。

### 7.8 回合结束（冲突保留）

- 方案 A：最后一张确认后自然回到只有 IP 的首页，不显示统计、成绩或自动续轮；
- 方案 B：进入全屏结算页，显示张数、用时、掌握变化，并提供“回到今天 / 看知识云”。

### 7.9 知识云（MVP 范围待确认）

交互说明方案包含云图、时间、平台三种模式，以及主题节点、筛选和搜索。产品草案将主题池、平台池、关键词筛选和卡牌管理边界列为未确认或 MVP 外能力。

若进入后续版本：三种模式在同页切换；节点点击进入主题集合；节点过多时聚合；空状态引导首次导入；核心数据必须是真实关系，不能用装饰性假图谱代替。

### 7.10 时间回忆相册（后续能力）

按年月浏览历史事件，支持月份切换、卡片详情和单卡重新唤醒；无内容时展示克制空状态；“一年前的今天”无数据时整体隐藏。

### 7.11 知识卡详情

展示等级或碎片状态、标题、来源状态、核心解释、原截图、上下文和关联卡。原截图是回溯证据，不是默认唤醒首屏。

支持查看来源、再次唤醒、复制和删除。删除成功后返回上一页并反馈；是否提供撤销需与“彻底删除”数据策略统一后确认。

### 7.12 分类与筛选（MVP 冲突）

交互说明方案提供今天待复习、上次中断、最新收下、主题、平台、随机重逢六项。产品草案明确 MVP 抽取前不选择领域、主题、平台或关键词。建议保留为后续能力，除非产品明确扩大 MVP。

### 7.13 搜索结果页（后续能力）

支持即时联想、最匹配/最近保存/最久未复习排序和结果详情。空结果提供相近主题或随机重逢，不形成死页。

### 7.14 提醒与设置

支持通知开关、每日时段、通知预览隐私、Reduce Motion 和系统权限状态。系统权限关闭时提供“前往设置”，但不阻断保存和唤醒。

---

## 8. 深层链接与状态恢复

| 来源 | 目标 | 规则 |
|---|---|---|
| 定向问题通知 | 对应卡片的待揭示状态 | 携带卡片 ID、内容版本、承重语义版本 |
| 今日回响通知 | 首页或默认候选 | 是否保留此通知类型待确认 |
| 时间回环通知 | 对应历史事件 | 后续能力 |
| 整理完成通知 | 新卡详情 | 与“不发生成完成通知”冲突，待确认 |

目标已删除时，安全降级到首页并提示内容已不存在，不能白屏或崩溃。

回合至少需要持久化：

- 回合 ID 与卡片顺序；
- 当前卡片和真实进度；
- 揭示阶段与刮开覆盖状态；
- 已提交反馈及幂等标识；
- 剩余卡片；
- 内容版本。

恢复时不得重新抽卡、重排顺序、重复提交反馈或重复播放完整入场动画。

---

## 9. 能力模块与实施边界

1. Capture Gateway：截图收件与可靠保存；
2. Processing Lifecycle：状态、重试、幂等、删除和恢复；
3. Screenshot Understanding：OCR、视觉理解和平台识别；
4. Source Resolver：候选发现、证据匹配和人工补源；
5. Context Builder：完整来源理解与截图片段定位；
6. Knowledge Card Generator：核心知识、承重语义和降级形态；
7. Quality Gate：来源支撑、意义完整性和遮盖质量；
8. Knowledge Value Engine：P/U/T/I/D/F 与稀有度；
9. Card Pool and Scheduler：今日候选、窗口、去重和补池；
10. Draw and Recall Experience：首页、卡叠、揭示、自评和恢复；
11. Card Completion Lifecycle：碎片、补源、升格、删除和版本；
12. Personalization Loop：根据反馈调整后续唤醒；
13. Recall Trigger：通知候选、隐私检查、每日上限和深链。

实施共同结论：

- 保留现有 iOS、后端、异步任务和内容提取框架；
- iOS 负责捕获、展示和交互，服务端负责溯源、理解、生成和质量检查；
- 体验重构不等于重写底层；
- 处理和事件写入必须幂等；
- 分析事件只记录必要元数据，不记录截图、OCR、核心知识、来源正文或通知问题文本。

---

## 10. 测试与验收重点

### 10.1 捕获与处理

- 只有可靠服务端收件或本地排队成功才显示“已收下”；
- 弱网、离线、退出、重启和网络切换后可恢复；
- 自动重试和手动重试不生成重复记录；
- 来源未找到与技术异常正确区分；
- 处理完成后首页和卡池状态一致；
- UI 不展示虚假百分比和内部流水线。

### 10.2 来源与知识质量

- 可溯源内容先建立完整上下文再生成；
- 记忆碎片不扩写截图之外的信息；
- 手动链接必须定位到截图内容才能升格；
- 知识结论和承重语义有来源证据；
- 遮盖不退化为随机关键词；
- 降级形态仍保留完整认知循环。

### 10.3 核心交互

- 刮动与点击揭示等价；
- 三档反馈只记录一次并保持幂等；
- 取消反馈不推进卡片；
- 中断后恢复当前卡、揭示状态、反馈状态和顺序；
- 一至十张回合不重复卡片；
- 最后一张行为符合最终选定方案；
- 小屏、横屏、最大 Dynamic Type、VoiceOver 和 Reduce Motion 均可完成。

### 10.4 调度与通知

- 近期、未知、中期、远期顺序稳定；
- 未反馈不视为想对；
- 稀有度不压过真实唤醒需求；
- 长期未出现卡不会永久饿死；
- 通知问题与目标版本严格匹配；
- 每个本地自然日最多一条，跨时区不重复；
- 删除或升格后旧通知安全失效；
- 通知拒绝不影响核心功能，也不反复请求权限。

### 10.5 删除与隐私

- 删除后卡片、截图、来源、历史、调度和未送达通知一致清除；
- 连续回合删除当前卡后顺序稳定且不补卡；
- 敏感卡锁屏不显示具体问题；
- 分析埋点不包含用户内容正文。

---

## 11. MVP 范围

### 11.1 两份文档共同支持的 MVP 能力

- 截图捕获与可靠异步收件；
- 视觉理解、来源恢复和上下文构建；
- 一张截图生成一张核心卡或记忆碎片；
- 统一卡池和动态候选；
- 轻量揭示与三档自我校准；
- 中断恢复、删除、通知隐私和无障碍；
- R / SR / SSR 的可信、克制表达。

### 11.2 产品草案明确排除的 MVP 能力

- 完整课程、章节、单元和线性学习路径；
- 一张截图生成多道题；
- 同时提供判断题、选择题、填空、拼图等多题型；
- 完整 SM-2、FSRS 和可视化遗忘曲线；
- 面向用户展示到期日、欠账和连续正确次数；
- 回合总结页、正确率、经验值和自动续轮；
- 单抽/十连模式选择和用户自选回合张数；
- 主题池、平台池、关键词筛选和抽取前分类；
- 付费抽卡、金币、体力、抽卡券和每日次数限制；
- 保底、强制签到、每日任务和未完成惩罚；
- 用户编辑 AI 卡片、填写正确答案、修正版确认和版本对比；
- 每张卡独立发送整理完成通知。

交互说明文档中与以上条目相反的页面和流程均已在冲突表保留，不能直接删除，也不能在未决策前进入默认 MVP 开发。

---

## 12. 成功标准与衡量

### 12.1 合格的二次唤醒

一次事件同时满足以下条件才计入：

1. 卡片来自此前可靠保存的截图；
2. 保存与本次揭示之间跨过预设有效延迟；
3. 用户完成语义揭示；
4. 用户提交一次三档自我校准。

即时查看不计为跨时段二次唤醒，但产品不因此阻止新卡生成后立即可抽。

### 12.2 核心指标

```text
观察期内至少完成一次合格二次唤醒的用户数
÷
拥有可复习卡且获得完整有效延迟机会的用户数
```

需要分别报告主动抽取与问题通知入口。

### 12.3 持续性与认知支持

- 不同日期重复完成唤醒的用户比例；
- “想偏了 → 想对了”“没想起 → 想对了”的恢复趋势；
- 连续多次想对后的稳定性；
- 近期窗口是否真正带来较早再次出现。

自我报告是趋势证据，不是客观考试成绩。

### 12.4 最终价值证据

通过访谈寻找具体的真实调用事件：用户是否在交流、面试、判断、创作或行动中成功想起并使用 Recallo 唤醒过的知识。“抽卡很好玩”或“以后可能有用”不算成功调用。

### 12.5 护栏

- 可靠收件率、最终丢失率、处理完成时间和重复卡率；
- 来源匹配、承重语义和记忆碎片质量；
- 揭示、自评和跨时段再次唤醒漏斗；
- 卡片删除率与删除阶段分布；
- 调度顺序、卡片饿死和稀有度公平性；
- 通知匹配失败、重复送达、隐私降级和关闭率。

截图数、抽卡量、回合完成率、会话时长和通知点击率只作为诊断指标，不能单独宣布 MVP 成功。

---

## 13. 冲突保留与待决策清单

| 编号 | 主题 | 方案 A：产品草案 | 方案 B：交互说明 | 状态 |
|---|---|---|---|---|
| C-01 | 视觉基准 |使用figma svg
| C-02 | 首页 | | 问候、主卡、中断、处理中、新发现等模块化首页 |
| C-03 | Share Extension | 提供 8 个一级主题选择 | 
| C-04 | 多候选来源 | 展示两个候选让用户选择 | 
| C-05 | 回合开始 | 点击 IP 直接进入卡叠 | 
| C-06 | 回合中断 | | 左上暂停、拦截返回、退出确认 |
| C-07 | 认知题型 | 语义刮开是+闪卡|
| C-08 | 自评后推进 | 节点松手后自动下一张 | 先弹反馈 Sheet，再点下一张 
| C-09 | 结果奖励 | 答对触发翠绿描边、黄色绒光、IP 短跳和回响值 | 
| C-10 | 回合结束 | 独立结算页，展示张数、用时、掌握变化 | 
| C-11 | 知识云 | 分类、搜索和管理边界未确认或 MVP 外 | 
| C-12 | 筛选抽取 | MVP 抽取前不选主题、平台或关键词 | 
| C-13 | 整理完成通知 | 不逐卡发送，只保留问题通知 
| C-14 | 单卡反馈层 | 不设置；次级信息非必经 |
| C-15 | 卡片详情删除撤销 |删除后提供 5 秒撤销 Toast |

在冲突未裁决前，设计稿、数据合同和测试用例应分别标注方案 A/B，不能把两套行为拼成一条默认流程。

---

## 14. 开放问题

1. MVP 最终采用极简首页还是模块化首页？
2. 点击 IP 是否直接进入卡叠，还是先显示回合开始页？
3. 回合是否允许产品内暂停/退出？
4. MVP 是否只保留语义刮开，还是同时支持闪卡、判断和选择？
5. 自评后是否自动下一张，还是必须经过反馈层？
6. 最后一张后是否需要结算页？
7. 知识云、时间相册、搜索和分类筛选是否进入 MVP？
8. Share Extension 是否展示主题选择？
9. 来源候选是否允许用户二选一，证据验证发生在选择前还是选择后？
10. 是否完全取消整理完成通知？
11. 翠绿/阳光黄是否为正式设计系统，银灰覆膜与其如何共存？
12. 删除后的“撤销”是软删除恢复，还是与彻底删除合同冲突？
13. 问题通知默认晚间时段采用哪个钟点？
14. 敏感内容识别边界和“仅通用通知”是否允许单卡设置？
15. “正在整理 N 条”、纯等待和暂未整理入口采用何种视觉形态？

---

## 15. 源文档章节映射

| 合并版章节 | `recallo-2-prd-draft.md` | `Recallo_功能与交互说明文档_v1.0(1).md` |
|---|---|---|
| 1 产品定位 | 1–4 | 页面目标与流程语境 |
| 2 术语 | 分散定义 | 一、术语对齐 |
| 3 信息架构 | 5.4、8、9 | 4.4、4.9–4.14 |
| 4 核心功能 | 5–6、8–9 | 三、功能流程图 |
| 5 全局规范 | 5.5、9 | 二、全局交互规范 |
| 6 功能流程 | 3、5 | 三、功能流程图 |
| 7 页面规格 | 5.1–5.7 | 四、页面级交互 |
| 8 深链与恢复 | 5.4、5.7、9 | 2.3、2.4 |
| 9 能力与实施 | 8、9 | 流程和页面实现说明 |
| 10 测试 | 10 | 五、状态速查 |
| 11 MVP 范围 | 11 | 4.1–4.14 中的扩展页面 |
| 12 衡量 | 12 | 未独立定义 |
| 13 冲突表 | 两文档差异 | 两文档差异 |
| 14 开放问题 | 13 | 页面方案中的未决范围 |

两份源文件保持原样，作为完整历史记录和追溯依据。
